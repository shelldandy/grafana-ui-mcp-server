# Environment variables for mcp-starter-typescript

# Node environment (development, production)
NODE_ENV=development

# Port for the HTTP/SSE server
# This is used by src/http.ts and referenced in compose.yaml
PORT=3000

# Example for a GitHub Personal Access Token, if your application needs one
# GITHUB_PERSONAL_ACCESS_TOKEN=your_github_pat_here
